"use client";

import { Fragment } from "react";
import { Toaster } from "react-hot-toast";

export default function ToastContainer() {
  return (
    <Fragment>
      <Toaster />
    </Fragment>
  );
}
